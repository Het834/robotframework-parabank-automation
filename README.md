# Parabank QA Automation Suite (Robot Framework + Selenium)

## 📌 Overview
This project automates functional, regression, and data-driven tests for the Parabank demo banking application using Robot Framework and Selenium.

## 📂 Project Structure
- `resources/` → reusable keywords
- `tests/` → test cases (login, transfers, bill pay, loans)
- `variables/` → credentials and environment configs
- `ci/` → GitHub Actions pipeline
- `reports/` → generated test reports

## 🚀 Setup
1. Clone the repo:
   ```bash
   git clone https://github.com/Het834/robotframework-parabank-automation